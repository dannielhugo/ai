{
  "name": "ai-monorepo",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "start:api": "yarn run start --workspace=ai-api",
    "start:app": "yarn run dev --workspace=ai",
    "build:api": "yarn run build --workspace=api",
    "build:app": "yarn run build --workspace=ai",
    "test:api": "yarn run test --workspace=api",
    "test:app": "yarn run test:unit --workspace=ai"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dannielhugo/ai-monorepo.git"
  },
  "workspaces": [
    "ai",
    "ai-api"
  ],
  "author": "",
  "private": true,
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/dannielhugo/ai-monorepo/issues"
  },
  "homepage": "https://github.com/dannielhugo/ai-monorepo#readme",
  "description": "",
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.9"
  }
}
